import sys
import pprint
import os
current_dir = os.path.dirname(os.path.realpath(__file__))
working_dir = os.path.join(current_dir , "..")
sys.path.append(working_dir)

#configures python path for src